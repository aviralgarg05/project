import React from 'react';

export const API_URL = 'http://10.0.2.2:5000';  // use localhost:5000 for iOS simulator or your LAN IP on a real device

// satisfy Expo Router’s requirement for a default export
export default function Config() {
  return null;
}
