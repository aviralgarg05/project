// Simple metro configuration for Expo projects
const { getDefaultConfig } = require('@expo/metro-config');

// Load and export Expo’s default Metro configuration
module.exports = getDefaultConfig(__dirname);
